apiVersion: v1
kind: Service
metadata:
  name: {{ include "3scale-dev.fullname" . }}
  labels:
    {{- include "3scale-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "3scale-dev.selectorLabels" . | nindent 4 }}
