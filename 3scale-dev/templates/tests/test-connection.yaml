apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "3scale-dev.fullname" . }}-test-connection"
  labels:
    {{- include "3scale-dev.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "3scale-dev.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
